import { Routes } from '@angular/router';
import { EditEntryComponent } from './edit-entry.component';

export const EDIT_ENTRY_ROUTES: Routes = [
  {
    path: '',
    component: EditEntryComponent,
  },
];
